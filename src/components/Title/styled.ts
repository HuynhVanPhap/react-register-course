import { styled } from "@mui/system";
import { 
    Box,
    Typography
} from "@mui/material";

export const Layout = styled(Box, {
    name: "Layout"
})(() => ({
    marginTop: "25px"
}));

export {
    Typography
}