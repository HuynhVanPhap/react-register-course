import * as Styled from "./styled";

export const Title = () => {
    return (
        <Styled.Layout>
            <Styled.Typography align="center" variant="h4">
                Title Component
            </Styled.Typography>
        </Styled.Layout>
    );
}