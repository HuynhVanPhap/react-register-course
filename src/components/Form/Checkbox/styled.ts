import { styled } from "@mui/system";
import {
    Box,
    FormControl,
    FormLabel,
    FormGroup,
    FormControlLabel,
    Checkbox
} from "@mui/material";

export const Layout = styled(Box, {
    name: "Layout"
})(() => ({
    
}));

export {
    FormControl,
    FormLabel,
    FormGroup,
    FormControlLabel,
    Checkbox
}
