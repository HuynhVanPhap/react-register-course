import { styled } from "@mui/system";
import { 
    FormControl,
    Box,
    InputLabel,
    Select,
    MenuItem
} from "@mui/material";

export const Layout = styled(Box, {
    name: "Layout"
})(() => ({
    
}));

export {
    FormControl,
    InputLabel,
    Select,
    MenuItem
}