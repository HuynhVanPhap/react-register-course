import { styled } from "@mui/system";
import { 
    FormControl,
    TextField,
    Box
} from "@mui/material";

export const Layout = styled(Box, {
    name: "Layout"
})(() => ({
    
}));

export {
    FormControl,
    TextField,
    Box
}